binder_call(radio, hal_audio_default)

allow radio aoc_device:chr_file { open read write };
allow radio hal_audio_ext_service:service_manager find;
allow radio imsstack_app:udp_socket { getattr read setopt shutdown write };
allow radio scheduling_policy_service:service_manager find;
